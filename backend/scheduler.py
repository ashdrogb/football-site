# Scheduler is no longer needed - all ESPN fetching happens client-side.
# Kept as a stub so existing imports don't break.

def start_scheduler(): pass
def stop_scheduler(): pass
def get_cache(): return {"scoreboards": {}, "standings": {}}
